@model IEnumerable<OPAS2Model.CostElement>

@{
  ViewBag.Title = "Index";
}

<h2>@Resources.Resource.MD_CostElement</h2>

<p>
  @Html.ActionLink(@Resources.Resource.G_Create, "Create")
</p>
<table class="table">
  <tr>
    <th>
      @Resources.Resource.MD_CostElementType
    </th>
    <th>
      @Resources.Resource.MD_CostElementAccountType
    </th>
    <th>
      @Resources.Resource.G_Name
    </th>
    <th>
      @Resources.Resource.MD_EnglishName
    </th>
    <th>
      @Resources.Resource.MD_Description
    </th>
    <th></th>
  </tr>

  @foreach (var item in Model)
  {
    <tr>
      <td>
        @Html.Action("showNameOfId", "CostElementType", 
          new { id = item.costElementTypeId })
      </td>
      <td>
        @Html.Action("displayCostElementAccountType", "EnumTypes", 
          new { id = (int)item.costElementAccountType })
        </td>
      <td>
        @Html.DisplayFor(modelItem => item.name)
      </td>
      <td>
        @Html.DisplayFor(modelItem => item.englishName)
      </td>
      <td>
        @Html.DisplayFor(modelItem => item.description)
      </td>

      <td>
        @Html.ActionLink(Resources.Resource.G_Edit, "Edit", 
          new { id = item.costElementId })
      </td>
    </tr>
  }

</table>
